//Crie um programa em C para gerar uma matriz 4 x 4 com valores no intervalo [1 a 20].
//Depois transforme a matriz gerada em uma matriz triangular inferior, ou seja, atribuindo zero
//a todos os elementos acima da diagonal principal, por fim imprima a matriz original e a matriz
//transformada.

#include <stdio.h>
#include <stdlib.h>
int main() {
	int matriz [4][4];
	int i,j;
	matriz [i][j]=rand()%100;
	for (i=0;i<4;i++) {
		for (j=0;j<4;j++)
		if (j>i) {
			matriz[j] = 0;
			printf("%i ",matriz [j]);
		} else {
			printf("%i ",matriz [j]);
		}
		printf("\n");
	}
}
